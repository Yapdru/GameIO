import * as THREE from 'https://unpkg.com/three@0.160.0/build/three.module.js';
function mat(c){return new THREE.MeshStandardMaterial({color:c,roughness:.55,metalness:.1});}
function cube(w,h,d,m){const x=new THREE.Mesh(new THREE.BoxGeometry(w,h,d),m);x.castShadow=true;x.receiveShadow=true;return x;}
function sphere(r,m){const x=new THREE.Mesh(new THREE.SphereGeometry(r,32,18),m);x.castShadow=true;return x;}
function makeAvatar(){const g=new THREE.Group();const skin=mat(0xffd6b5),shirt=mat(0x0f8fe8),pants=mat(0x12304a),shoe=mat(0x111111);const head=sphere(.42,skin);head.position.y=2.05;const hair=sphere(.44,mat(0x1d1d1d));hair.scale.y=.45;hair.position.y=2.28;const body=cube(.9,1.05,.44,shirt);body.position.y=1.25;const armL=cube(.24,.9,.24,shirt);armL.position.set(-.67,1.25,0);armL.rotation.z=.18;const armR=armL.clone();armR.position.x=.67;armR.rotation.z=-.18;const legL=cube(.28,.88,.28,pants);legL.position.set(-.25,.45,0);const legR=legL.clone();legR.position.x=.25;const shoeL=cube(.38,.14,.55,shoe);shoeL.position.set(-.25,-.05,.08);const shoeR=shoeL.clone();shoeR.position.x=.25;g.add(head,hair,body,armL,armR,legL,legR,shoeL,shoeR);return g;}
export function startAvatarPreview(el){if(!el||el.dataset.on)return;el.dataset.on='1';el.innerHTML='';const scene=new THREE.Scene();scene.background=new THREE.Color(0xe9fbff);const cam=new THREE.PerspectiveCamera(45,1,.1,100);cam.position.set(0,1.7,5);const ren=new THREE.WebGLRenderer({antialias:true});ren.setSize(260,260);ren.setPixelRatio(Math.min(devicePixelRatio,2));ren.shadowMap.enabled=true;el.appendChild(ren.domElement);scene.add(new THREE.HemisphereLight(0xffffff,0x9edfff,1.7));const sun=new THREE.DirectionalLight(0xffffff,2);sun.position.set(3,5,4);sun.castShadow=true;scene.add(sun);const floor=new THREE.Mesh(new THREE.CircleGeometry(1.6,64),new THREE.MeshStandardMaterial({color:0xbff4ff,roughness:.8}));floor.rotation.x=-Math.PI/2;floor.receiveShadow=true;scene.add(floor);const av=makeAvatar();scene.add(av);function loop(){av.rotation.y+=.015;av.position.y=Math.sin(performance.now()/500)*.04;ren.render(scene,cam);requestAnimationFrame(loop)}loop();}
window.GameIOAvatar3D={startAvatarPreview};
setTimeout(()=>{startAvatarPreview(document.getElementById('avatar3DMount'));startAvatarPreview(document.getElementById('joinAvatar3DMount'));},600);
